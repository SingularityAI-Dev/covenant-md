{
  "name": "covenant-md-benchmarks",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Eval harness for the COVENANT.md contract thesis (interface adherence, output fidelity, undeclared side effects).",
  "scripts": {
    "bench": "node run.mjs",
    "smoke": "node run.mjs --task tasks/interface-adherence --adapter mock --trials 3"
  },
  "dependencies": {
    "@covenant-md/core": "^1.0.0",
    "js-yaml": "^4.1.0"
  },
  "optionalDependencies": {
    "@anthropic-ai/sdk": "^0.30.0"
  }
}
