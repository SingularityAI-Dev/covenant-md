# template-rendering

Apply a template string to a content object.

## Operations

- `render(template, content) → rendered`, substitute placeholders.
- `validate_template(template) → is_valid`, check template syntax.

This is a contract-only example skill: the COVENANT.md is the artifact. There is no implementation script; the framework's contract-driven simulator handles its fixtures.
